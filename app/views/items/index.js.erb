$(".updated").html('<%= j select_tag(:subcategory,options_from_collection_for_select( @category.subcategories.all , :id , :name , @subcategory.nil? ? "" : @subcategory.id) , {prompt: "Select Subcategory" , onchange: "$(\'#buga\').submit()"}) %>');
<% if @subcategory.nil? %>
	$(".updated2").html('<%= j select_tag(:brand,options_from_collection_for_select( [] , :id , :name , @category.nil? ? "" : @category.id) , {prompt: "Select Brand" , onchange: "$(\'#buga\').submit()" }) %>');	
<% end %>
<% unless @subcategory.nil?  %>
	$(".updated2").html('<%= j select_tag(:brand,options_from_collection_for_select( @subcategory.brands.all , :id , :name , @brand.nil? ? "" : @brand.id) , {prompt: "Select Brand" , onchange: "$(\'#buga\').submit()"}) %>');	
<% end %>
$(".updated3").html("<%= j render 'list' %>");